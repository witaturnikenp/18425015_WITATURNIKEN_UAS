<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".user.ProfileuserActivity"></activity>
        <activity android:name=".user.HistoriTransaksiActivity" />
        <activity android:name=".user.DetailActivity" />
        <activity android:name=".user.DashboarduserActivity" />
        <activity android:name=".loginregis.RegisActivity" />
        <activity android:name=".admin.profilActivity" />
        <activity android:name=".admin.inputDataActivity" />
        <activity android:name=".admin.EditdanHapusActivity" />
        <activity android:name=".admin.DataTransaksiActivity" />
        <activity android:name=".admin.DashboardAdminActivity" />
        <activity android:name=".admin.DataActivity" />
        <activity android:name=".loginregis.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>